{
  "name": "ps-besinky",
  "private": true,
  "scripts": {
    "docker:start": "docker compose up",
    "docker:start:f-build": "yarn docker:start --build"
  },
  "packageManager": "yarn@4.12.0",
  "workspaces": [
    "applications/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3"
  }
}
